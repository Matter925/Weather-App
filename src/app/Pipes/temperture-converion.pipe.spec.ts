import { TempertureConverionPipe } from './temperture-converion.pipe';

describe('TempertureConverionPipe', () => {
  it('create an instance', () => {
    const pipe = new TempertureConverionPipe();
    expect(pipe).toBeTruthy();
  });
});
